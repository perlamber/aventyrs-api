# Project-wide Gradle settings.
# Do NOT put machine-specific values here (e.g. org.gradle.java.home) - this file
# is committed and is shared by every machine that builds the project.
# The JDK used for compilation is pinned by the toolchain block in build.gradle.
